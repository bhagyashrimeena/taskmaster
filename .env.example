# Backend configuration is loaded from backend/.env.
# Copy backend/.env.example to backend/.env for the full backend setup.

# Optional frontend override for the backend API. The default is http://127.0.0.1:8001.
WEALTH_COPILOT_BACKEND_URL=http://127.0.0.1:8001